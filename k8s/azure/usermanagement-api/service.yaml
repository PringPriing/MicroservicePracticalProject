apiVersion: v1
kind: Service
metadata:
  name: usermanagement-api
  namespace: microservices
spec:
  type: LoadBalancer
  selector:
    app: usermanagement-api
  ports:
    - port: 8081
      targetPort: 8080
