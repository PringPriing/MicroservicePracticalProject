apiVersion: apps/v1
kind: Deployment
metadata:
  name: productcatalog-api
  namespace: microservices
spec:
  replicas: 1
  selector:
    matchLabels:
      app: productcatalog-api
  template:
    metadata:
      labels:
        app: productcatalog-api
    spec:
      containers:
        - name: productcatalog-api
          image: productcatalog-api:local
          imagePullPolicy: Never
          ports:
            - containerPort: 8080
          envFrom:
            - configMapRef:
                name: productcatalog-api-config
            - secretRef:
                name: productcatalog-api-secret
          readinessProbe:
            tcpSocket:
              port: 8080
            initialDelaySeconds: 10
            periodSeconds: 10
          livenessProbe:
            tcpSocket:
              port: 8080
            initialDelaySeconds: 20
            periodSeconds: 20
          resources:
            requests:
              memory: "256Mi"
              cpu: "250m"
            limits:
              memory: "512Mi"
              cpu: "1"
